class Solution {
public:
    bool checkTree(TreeNode* root) {
        int left = root->left->val;
        int right = root->right->val;
        if(left+right == root->val)
        {
            return true;
        }
        return false;
    }
};
